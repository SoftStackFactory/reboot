language: node_js
node_js:
  - "8.9.4"

branches:
  only:
    - master
    - dev

before_script:
  - npm install -g ionic cordova jest

script:
  - npm run test-ci

deploy:
  provider: heroku
  api_key:
    secure: KEY
  app: ssf-reboot
  on:
    repo: SoftStackFactory/reboot
